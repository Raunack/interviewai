# Copy to .env.local and fill in values.

GROQ_API_KEY=

# Server-side (keep secret; never expose to the browser)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Client-safe (used by the browser + middleware for Supabase Auth)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
